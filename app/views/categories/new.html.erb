<!-- Título principal -->
<h1 class="text-2xl font-bold text-gray-800 mb-6"><%= t('.title') %></h1>

<!-- Renderizado del formulario de la categoría -->
<div class="bg-white shadow-md rounded-lg p-6">
  <%= render "form", category: @category %>
</div>

<!-- Espaciado entre el formulario y el enlace -->
<div class="mt-6">
  <%= link_to t('.back'), categories_path, class: "text-primary hover:underline" %>
</div>
